title = "Minimum Number of Operations to Move All Balls to Each Box "

arr = title.split(" ")
res = ""
for word in arr:
    res += word.lower() + "_"
    name = res[:-1] + ".py"

print(res[:-1] + ".py")

file = open(name , 'w')
file.close()

